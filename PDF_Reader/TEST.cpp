
#include "PDF_Reader.h"
#include <string>
#include "renderPDF.h"
#include <WinUser.h>
/*
int main() 
{
	char path[] = "Hello This is a Test";
	FPDF_DOCUMENT doc = initPDFium(path);
	FPDF_PAGE p1 = FPDF_LoadPage(doc, 0);
	FPDF_RenderPage(GetDC(NULL), p1, 0, 0, 100, 100, 0, 0);
	freePDFium();
}
*/